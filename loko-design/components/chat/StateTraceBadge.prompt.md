StateTraceBadge — mono pill showing the exact state machine node the bot is in, for admin/debug views and the widget's "why this answer" affordance.

```jsx
<StateTraceBadge state="state.confirm_return" confidence={0.94} />
```

Use in the admin console's conversation log and, optionally, behind an "i" disclosure in the widget itself.
