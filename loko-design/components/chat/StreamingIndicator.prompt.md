StreamingIndicator — calm three-dot "bot is composing" indicator, no bounce/glow.

```jsx
<StreamingIndicator />
```

Deliberately restrained animation (opacity pulse only) — LOKO is not a "thinking AI" gimmick, it's retrieving and composing a traceable answer.
