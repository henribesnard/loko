---
name: loko-design
description: Use this skill to generate well-branded interfaces and assets for LOKO (B2B chatbot platform, deterministic/RAG, EU data sovereignty positioning), for production or throwaway prototypes/mocks. Contains logo, colors, type, chat-widget components, and brand board.
user-invocable: true
---

Read `readme.md` in this skill first, then explore the other files.

If creating visual artifacts (slides, mocks, throwaway prototypes), copy assets out (`assets/logo/`, tokens) and build static HTML using the CSS custom properties in `styles.css` — never hardcode colors. If working on production code, copy the `components/chat/` primitives and read `readme.md`'s VISUAL FOUNDATIONS section to stay consistent.

If invoked without other guidance, ask what to build, ask a few clarifying questions, and act as an expert designer for LOKO — sober, precise, deterministic tone; avoid AI clichés (gradients, sparkles, neon, robots).
