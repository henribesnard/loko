# constraints-ml.txt — ML dependency pins for reproducible builds.
#
# Referenced in Dockerfile: pip install -c constraints-ml.txt
# Regenerate after validating a new environment:
#   pip freeze | grep -iE 'setfit|transformers|sentence-transformers|torch|huggingface|tokenizers|safetensors' > constraints-ml.txt
#
setfit==1.1.3
sentence-transformers==3.3.1
transformers==4.46.3
huggingface-hub==0.26.5
tokenizers==0.20.3
safetensors==0.4.5
